name: (MCP) Build & Test

on:
  push:
    branches: [main]
    paths:
      - 'src/Everywhere.Mcp/**'
      - 'src/Everywhere.Abstractions/**'
      - 'src/Everywhere.Core/**'
      - 'src/Everywhere.Terminal/**'
      - 'tests/Everywhere.Mcp.Tests/**'
      - '3rd/open-connector/**'
      - 'scripts/build-connector-bundle.mjs'
      - 'scripts/verify-connector-bundle.mjs'
      - 'package.json'
      - 'Directory.Build.props'
      - 'Directory.Packages.props'
      - 'global.json'
      - '.github/workflows/mcp-ci.yml'
  pull_request:
    paths:
      - 'src/Everywhere.Mcp/**'
      - 'src/Everywhere.Core/**'
      - 'tests/Everywhere.Mcp.Tests/**'
      - '3rd/open-connector/**'
      - 'scripts/build-connector-bundle.mjs'
      - 'scripts/verify-connector-bundle.mjs'
      - 'package.json'
      - 'Directory.Packages.props'
      - '.github/workflows/mcp-ci.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: build-and-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: true
          # LFS off — open-codex-computer-use zip blows the bandwidth
          # quota and MCP CI doesn't need it (matches release workflows).
          lfs: false

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.202'

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      # Connector bundle must exist before `dotnet build` runs — the C#
      # runtime probes the emitted dist/ at boot. SPEC everywhere-connector.md §5.
      - name: Install node deps (connector bundle)
        run: npm ci || npm install
        working-directory: ${{ github.workspace }}

      - name: Build connector bundle
        run: node scripts/build-connector-bundle.mjs
        working-directory: ${{ github.workspace }}

      # SPEC docs/specs/everywhere-connector.md §10.2 — assert the
      # bundle's runtime provider set matches what the manifest declares.
      # Catches upstream renames or vendor typos that would otherwise
      # produce a "list says X but run fails with unknown_action" bug.
      - name: Verify connector bundle/manifest agreement
        run: node scripts/verify-connector-bundle.mjs
        working-directory: ${{ github.workspace }}

      # Web Console (React SPA) — vendored under 3rd/open-connector/web,
      # emits into 3rd/open-connector/dist/web which MSBuild copies to
      # Resources/connector/web/ for Kestrel to serve at /connector-ui/.
      - name: Install web deps
        run: npm install --no-audit --no-fund
        working-directory: 3rd/open-connector/web

      - name: Build connector web console
        run: npx vite build
        working-directory: 3rd/open-connector/web

      - name: Restore
        run: dotnet restore src/Everywhere.Mcp/Everywhere.Mcp.csproj

      - name: Restore tests
        run: dotnet restore tests/Everywhere.Mcp.Tests/Everywhere.Mcp.Tests.csproj

      - name: Build (Everywhere.Mcp)
        run: dotnet build src/Everywhere.Mcp/Everywhere.Mcp.csproj -c Release --no-restore

      - name: Build (Everywhere.Mcp.Tests)
        run: dotnet build tests/Everywhere.Mcp.Tests/Everywhere.Mcp.Tests.csproj -c Release --no-restore

      - name: Test
        run: dotnet test tests/Everywhere.Mcp.Tests/Everywhere.Mcp.Tests.csproj -c Release --no-build --logger "console;verbosity=normal"
